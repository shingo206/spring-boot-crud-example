spring:
  datasource:
    username: postgres
    password: VLw&QgM*293VuYi
    url: jdbc:postgresql://localhost:5435/test
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true


logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace
